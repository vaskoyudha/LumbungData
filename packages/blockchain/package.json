{
  "name": "@repo/blockchain",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test:unit": "vitest run --config vitest.config.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "happy-dom": "^20.8.4",
    "typescript": "^5.0.0",
    "vitest": "^4.1.0"
  }
}
